assignableWateringPeriod: WateringPeriod @cypher(statement:
"""
MATCH (t:WateringTask)-[:within]-(p:WateringPeriod)
WHERE not(exists((t)-[:assigned]-(:User))) AND date() < date(p.till)
RETURN p
"""
)
